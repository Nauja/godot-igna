# Enums accessible from everywhere
extends Node

# List of screens in the game
enum EScreen { TITLE, LEVEL }

# Possible directions
enum EDirection { LEFT, RIGHT, UP, DOWN }

# Modules of the rover
enum EModule { POWER, ENGINE }

# Possible maps in a level
enum EMap { WORLD, ROVER, ROCKET }
