class_name EntitySheet
extends Resource

# Hint when cursor is over
@export_subgroup("EntitySheet")
@export var cursor_hint: String
# Cursor priority when on the same tile as other entities
@export var cursor_priority: int
